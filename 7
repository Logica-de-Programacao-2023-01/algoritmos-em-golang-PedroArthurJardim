package main

import "fmt"

func main() {
	var x int
	fmt.Print("Qual é o número?")
	fmt.Scan(&x)

	var antecessor = x - 1
	sucessor := x + 1
	fmt.Print("Seu antecesor é ", antecessor, " e seu sucessor é ", sucessor)
}
